program print

bits 64

feature enable shorten_syscalls
feature enable append_str_length

section .data
    msg db "This is GHLA! Welcome!", 10

section .text

_start:
    syscall 1, 1, msg, msg_len
    syscall 60, 0

export _start
