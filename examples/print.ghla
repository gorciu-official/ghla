program print

bits 64

feature enable shorten_syscalls
feature enable append_str_length
feature enable syscall_constants

section .data
    msg db "This is GHLA! Welcome!", 10

section .text

export _start:
    syscall SYSCALL_WRITE, 1, msg, msg_len
    syscall SYSCALL_EXIT, 0