program print

bits 64

feature enable shorten_syscalls

section .data
    msg db "Hello from GHLA!", 10
    msg_len equ $ - msg

section .text

_start:
    syscall 1, 1, msg, msg_len

    mov rax, 60
    xor rdi, rdi
    syscall

export _start
